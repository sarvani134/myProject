<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Not Enrolled</title>
    <!-- <link href="/style.css" /> -->
    <link rel="stylesheet" href="/style.css" />
</head>
<body>
    <h1>Not Enrolled students</h1>
    <% for(student of students){%>

          
            
             <% if(student.isEnrolled===false){ %>
                <div class="box">
                <h3>Name <%= student.name %></h3>
                <h3>Id: <%= student.id %></h3>
                <% } %>
          </div>
        <% } %>
        <a href="/students" >Go back to home page</a>
</body>
</html>